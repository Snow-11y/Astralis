package stellar.snow.astralis.engine.render.particles;
import org.joml.*;
    public static class EmitterConfig {
        Vector3f position;
        float emissionRate;
        float lifetime;
        float speed;
    }
    public void emit(EmitterConfig config, float deltaTime) {
        // Spawn particles
    }
}
